import 'package:flutter/material.dart';

class FluxPage extends StatelessWidget {
  const FluxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Flux d'activités")),
    );
  }
}
